class HTMLTag{
	constructor(tag_type, tag_content, tag_styling, tag_attributes){
		this.tag_type = tag_type;
		this.tag_content = tag_content;
		this.tag_styling = tag_styling;
		this.tag_attributes = tag_attributes;

		this.innerHTMLPossible = () => {

		}
		this.populateFrame = () => {/* Do something here */}

	}

}

//Same thing, but..... 
var p_tag = new HTMLTag('p', 'foo_bar', 'display:inline-block;', '{onClick : this.blink()}');


class HTMLTagContaining{

}

class HTMLTagNotContaining{

}


class HTMLTagBuilder{

}


HTMLFactory(){

	let type;
	/*==============
	Determine if the HTML tag can contain "inner text"
	==============*/


	let type = inner_text

	//Determine if the HTML tag is a "container" tag (ex: <select>)


	let let type = inner_text

	/* Define properties about this that make sense here  */

	//Determine if the HTML tag is a ".... derp tag"



	let type_tag extends HTMLTag{

	}

	return 

}